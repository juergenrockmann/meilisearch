api:
  insecure: true        # DEV ONLY
  dashboard: true

log:
  level: INFO

entryPoints:
  web:
    address: ":80"

providers:
  docker:
    endpoint: "unix:///var/run/docker.sock"
    exposedByDefault: false

# Optional: access logs (useful in dev)
accessLog: {}
