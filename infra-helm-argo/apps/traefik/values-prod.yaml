deployment:
  replicas: 2

ingressRoute:
  dashboard:
    enabled: false

certificatesResolvers:
  letsencrypt:
    acme:
      email: ops@example.com
      storage: /data/acme.json
      httpChallenge:
        entryPoint: web
