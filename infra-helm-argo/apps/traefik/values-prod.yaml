deployment:
  replicas: 2

ingressRoute:
  dashboard:
    enabled: false
  ingress:
    hosts:
      - host: traefik.local
        paths:
          - "/"

certificatesResolvers:
  letsencrypt:
    acme:
      email: ju.rockmann@gmail.com
      storage: /data/acme.json
      httpChallenge:
        entryPoint: web
